rule null_to_nullptr {
  match:
    node: NullLiteral
  replace:
    nullptr
}

rule bfs_expansion {
  match:
    node: BFS
  generate:
  <<<
    {
      std::queue<Node*> q;
      q.push(&${from});
      ${from}.visited = true;
      while (!q.empty()) {
        Node* parent = q.front(); q.pop();
        for (Node* neighbor : parent->neighbors) {
          if (!neighbor->visited) {
            neighbor->visited = true;
            q.push(neighbor);
          }
        }
      }
    }
  >>>
}